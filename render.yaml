# Blueprint de Render: despliega EcoRed como un solo servicio Node.
# (Express sirve la API en /api y la app React compilada en frontend/dist.)
services:
  - type: web
    name: ecotech-pro
    runtime: node
    plan: free
    buildCommand: npm run build
    startCommand: npm start
    autoDeploy: true
